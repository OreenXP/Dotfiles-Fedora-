#!/usr/bin/env bash

lock_file="${XDG_RUNTIME_DIR:-/tmp}/waybar-impala.lock"
exec 9>"${lock_file}"
flock -n 9 || exit 0

cd "${HOME}" || exit 1
exec kitty --class impala -e "${HOME}/.cargo/bin/impala"
