#!/bin/sh

image_dir="${HOME}/Imágenes/Iconos kitty"
image=""
if [ -d "$image_dir" ]; then
  image=$(find "$image_dir" -maxdepth 1 -type f -iname '*.jpeg' | shuf -n 1)
fi
[ -z "$image" ] || set -- --logo-type kitty -l "$image" "$@"
exec /usr/bin/fastfetch "$@"
